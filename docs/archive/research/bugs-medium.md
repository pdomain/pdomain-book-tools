# Medium-Severity Bugs

Bugs that produce incorrect behavior in realistic inputs, cause crashes on non-trivial
edge cases, or create subtle silent failures. Should be fixed before the next release.

---

**Status (2026-05-07):** all 30 medium-severity items have been
processed. Closed entries (FIXED) live in
[`archive/bugs-medium.md`](archive/bugs-medium.md).

When a new medium-severity bug is identified, add it as a `M-NN` entry
in this file. When it's closed, **move** the entry into
`archive/bugs-medium.md` rather than annotating it FIXED in place —
see [`README.md`](README.md) for the workflow.
