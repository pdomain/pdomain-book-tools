# High-Severity Bugs

Critical bugs that cause data corruption, definite crashes in reachable code paths, or
silently produce wrong output. Fix these before any new feature work.

---

**Status (2026-05-07):** all 21 high-severity items have been processed.
Closed entries (FIXED / STALE) live in
[`archive/bugs-high.md`](archive/bugs-high.md).

When a new high-severity bug is identified, add it as a `H-NN` entry in
this file. When it's closed, **move** the entry into
`archive/bugs-high.md` rather than annotating it FIXED in place — see
[`README.md`](README.md) for the workflow.
