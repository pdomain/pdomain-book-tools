# Low-Severity Bugs

Minor correctness issues, edge-case failures, unhelpful error messages, and behavioral
inconsistencies. Worth fixing but not blocking.

---

**Status (2026-05-07):** all 40 low-severity items have been processed.
Closed entries (FIXED, plus L-23 / L-38 marked STALE / DEFERRED with
regression locks) live in [`archive/bugs-low.md`](archive/bugs-low.md).

When a new low-severity bug is identified, add it as a `L-NN` entry in
this file. When it's closed, **move** the entry into
`archive/bugs-low.md` rather than annotating it FIXED in place — see
[`README.md`](README.md) for the workflow.
